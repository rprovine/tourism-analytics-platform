services:
  - type: web
    name: tourism-analytics-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app-ultra-minimal:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0