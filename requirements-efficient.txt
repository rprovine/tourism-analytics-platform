# Efficient requirements that build on smaller instances
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
jinja2==3.1.2
aiofiles==23.2.1
python-dotenv==1.0.0

# Lightweight analytics (no heavy ML dependencies)
vaderSentiment==3.3.2

# Use these only if memory allows (comment out if build fails)
# pandas==2.1.3
# numpy==1.25.2  
# scikit-learn==1.3.2